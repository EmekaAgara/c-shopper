import { StyleSheet, Text, View } from "react-native";
import React from "react";

const swap = () => {
  return (
    <View>
      <Text>swap</Text>
    </View>
  );
};

export default swap;

const styles = StyleSheet.create({});
